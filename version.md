# Version log

## 0.0.7 — 2026-06-08

- Removed decorative, CTA, navigation, FAQ, and social icons across the landing page per executive visual feedback.
- Simplified `AccentRail` into a strictly text-first primitive without an icon slot.
- Replaced icon affordances with typography, text labels, simple bullets, and `+` / `−` FAQ state symbols.
- Removed the unused `lucide-react` dependency.
- Startup version log and package metadata bumped `0.0.6` → `0.0.7`.

## 0.0.6 — 2026-06-07

- Commented out the `AdoptionGap.tsx` chart and metric-card infographic block per boss feedback.
- Kept the section headline, explanatory copy, and closing statement visible.
- Startup version log and package metadata bumped `0.0.5` → `0.0.6`.

## 0.0.5 — 2026-06-07

- Commented out the bordered supporting paragraph block under the main hero headline in `Hero.tsx` per boss feedback.
- Kept the headline, CTAs, and static hero image unchanged.
- Startup version log and package metadata bumped `0.0.4` → `0.0.5`.

## 0.0.4 — 2026-06-07

- `AccentRail` normalized so eyebrow labels and icons render strictly in white across the redesigned landing-page sections.
- Accent colors remain only in the vertical line, accent dot, and supporting subtle frame details.
- Startup version log and package metadata bumped `0.0.3` → `0.0.4`.

## 0.0.3 — 2026-06-07

- Introduced the reusable `AccentRail` component for text-first landing-page content.
- Replaced heavy rectangular content cards with vertical accent-line blocks across the main high-visibility sections.
- Kept charts, screenshots, and product visuals in lighter framed containers where visual structure is still needed.
- Startup version log and package metadata bumped `0.0.2` → `0.0.3`.

## 0.0.2 — 2026-06-07

- Adoption gap section rebalanced: the left chart panel is slightly wider on large screens.
- Right-side metric cards in `AdoptionGap.tsx` now use increased padding and line-height so their total visual height better matches the chart.
- Startup version log and package metadata bumped `0.0.1` → `0.0.2`.

## 0.0.1 — 2026-06-07

- Hero: removed framed card and 3-photo carousel; now a single static `HexaArm_new` visual with a soft glow.
- Axis 5 reducer photo added as visible-mechanics proof inside the **Visible Architecture** card (`ConnectedPlatform.tsx`).
- `servo-gear-blueprint.png` in `BuiltAsOneSystem.tsx` left unchanged.
- Version bumped `0.0.0` → `0.0.1`; version now logged to the browser console on startup.

## 0.0.0

- Initial version.
